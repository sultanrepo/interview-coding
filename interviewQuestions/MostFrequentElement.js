const arr = [1, 3, 2, 1, 4, 1, 3, 3, 3];

function mostFrequestElement(arr) {
    const freqMap = {};
    let maxCount = 0;
    let mostRrequest = null;
    for(let el of arr) {
        //freqMap[el] = ()
    }
}